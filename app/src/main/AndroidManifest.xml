<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.johnta.homeworkappv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                s
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlannerActivity" />
        <activity android:name=".CloudActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".FirstCreateActivity" />
        <activity
            android:name=".PlannerPopup_popup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".NewUserPopupActivity" />
        <activity
            android:name=".LoginPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".DeleteObjectFromArray" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".MakeScheduleURL_popup" />
        <activity android:name=".Error404_popup" />
        <activity android:name=".ErrorGeneral_popup"></activity>
    </application>

</manifest>