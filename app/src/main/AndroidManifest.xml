<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.johnta.homeworkappv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                s
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlannerActivity" />
        <activity android:name=".CloudActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".FirstCreateActivity" />
        <activity
            android:name=".popup.PlannerPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".NewUserPopupActivity" />
        <activity
            android:name=".popup.LoginPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".DeleteObjectFromArray" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".popup.CreateScheduleURLPopup" />
        <activity android:name=".popup.Error404Popup" />
        <activity android:name=".popup.ErrorGeneralPopup" />
        <activity android:name=".PersonalSchedule" />
        <activity android:name=".popup.RemoveItemFromListPopup" />
        <activity android:name=".popup.CloudPopup"></activity>
    </application>

</manifest>